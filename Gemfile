source "http://rubygems.org"

# Declare your gem's dependencies in schedule_fu.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec

# jquery-rails is used by the dummy application
gem "jquery-rails"

# rails_sql_views is used for generating the migration into the app schema.rb
# The github fork referenced here integrates some patches for rails 3.2 and
# mysql2 support.
# REVIEW: link to published version after these pull requests are integrated:
#   https://github.com/activewarehouse/rails_sql_views/pull/12
#   https://github.com/activewarehouse/rails_sql_views/pull/11
#   https://github.com/activewarehouse/rails_sql_views/pull/8
gem "rails_sql_views", :git => "https://github.com/avit/rails_sql_views.git"

# Declare any dependencies that are still in development here instead of in
# your gemspec. These might include edge Rails or gems from your path or
# Git. Remember to move these dependencies to your gemspec before releasing
# your gem to rubygems.org.

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
